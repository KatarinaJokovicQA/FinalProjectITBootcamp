package org.Selenium.pom.pages;

import org.Selenium.pom.base.WithChromeDriver;
import org.openqa.selenium.WebDriver;

public class ContactUsPage extends WithChromeDriver {
    public ContactUsPage(WebDriver driver) {
        super(driver);
    }
}
